name: CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo "Deploying app..."
      - run: echo "Secret: ${{ secrets.DEPLOY_KEY }}"
